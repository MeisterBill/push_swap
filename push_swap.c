#include "../includes/push_swap.h"

int	main(int ac, char **argv)
{
	t_pile	*pile_a;
	t_pile	*pile_b;
	int			stacksize;
	char		**tab;

	if (ac < 2)
		return (0);
	tab = filltab_and_checks(ac, argv);
	
}
